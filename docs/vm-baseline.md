# Sleepy VM Baseline

- VM name: `Sleepy`
- Capture date: 2026-08-23
- System state version: `26.05`
- Architecture: `x86_64-linux`
- Disk bus: virtio (libvirt target `vda`)

`system.stateVersion` is a migration contract. Future upgrades must never change it automatically; changing it requires an explicit, separately reviewed migration.
